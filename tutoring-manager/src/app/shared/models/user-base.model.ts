export class UserBase {
  id: string;
  name: string;
  age: number;
  gender: number; //1 = male; 0 = female
  yearOfSchool: string; //only for students
  teachedSubjects: string; //only for teachers
}